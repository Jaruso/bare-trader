"""Pydantic schemas for BareTrader.

These models define the I/O contracts for the app service layer.
Each schema has a `from_domain()` classmethod to convert from
existing dataclasses without replacing them.
"""
